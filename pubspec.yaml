name: hikaronsfa
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.2+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  dio: ^5.6.0
  shared_preferences: ^2.0.18
  flutter_bloc: ^8.1.2
  awesome_dialog: ^3.0.2
  intl: ^0.19.0
  flutter_easyloading: ^3.0.5
  dropdown_search: ^5.0.6
  flutter_slidable: ^3.1.0
  loading_animation_widget: ^1.2.1
  animated_bottom_navigation_bar: ^1.3.3
  persistent_bottom_nav_bar: ^6.2.1
  auto_size_text: ^3.0.0
  badges: ^3.1.2
  flutter_map: ^8.2.2
  flutter_map_dragmarker: ^8.0.3
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  local_auth: ^2.3.0
  file_picker: ^8.1.2
  image_picker: ^1.1.2
  image: ^4.7.2
  permission_handler: ^11.3.1
  shimmer: ^3.0.0
  animated_text_kit: ^4.2.2
  url_launcher: ^6.3.1
  month_picker_dialog: ^5.1.3
  path_provider: ^2.1.5
  camera: ^0.10.5+9
  uuid: ^4.5.2
  expansion_tile_list: ^2.0.0
  advanced_searchable_dropdown: ^2.1.0
  carousel_slider: ^5.1.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
  - assets/ca/lets-encrypt-r3.pem
  - assets/images/hikaron.jpg
  - assets/images/hikaron copy.jpg
  - assets/images/AccountMale.png
  - assets/images/Home.png
  - assets/images/HomeGrey.png
  - assets/images/Calendar.png
  - assets/images/CalendarGrey.png
  - assets/images/Alarm.png
  - assets/images/AlarmGrey.png
  - assets/images/People.png
  - assets/images/PeopleGrey.png
  - assets/images/Dashboard.png
  - assets/images/user_avatar.png
  - assets/images/Buy.png
  - assets/images/Clipboard.png
  # BANNER
  - assets/banner/1.png
  - assets/banner/2.png
  - assets/banner/3.png
  - assets/banner/about-us.png
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: MontserratRegular
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat/Montserrat-Regular.ttf
    - family: MontserratSemiBold
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat/Montserrat-SemiBold.ttf
    - family: MontserratMedium
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat/Montserrat-Medium.ttf
    - family: MontserratBold
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat/Montserrat-Bold.ttf
    - family: MontserratBlack
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat/Montserrat-Black.ttf
    - family: JakartaSansRegular
      fonts:
        - asset: assets/fonts/JakartaSans/PlusJakartaSans-Regular.ttf
    - family: JakartaSansSemiBold
      fonts:
        - asset: assets/fonts/JakartaSans/PlusJakartaSans-SemiBold.ttf
    - family: JakartaSansMedium
      fonts:
        - asset: assets/fonts/JakartaSans/PlusJakartaSans-Medium.ttf
    - family: JakartaSansBold
      fonts:
        - asset: assets/fonts/JakartaSans/PlusJakartaSans-Bold.ttf
    - family: InterRegular
      fonts:
        - asset: assets/fonts/Inter/Inter-Regular.otf
    - family: InterSemiBold
      fonts:
        - asset: assets/fonts/Inter/Inter-SemiBold.otf
    - family: InterMedium
      fonts:
        - asset: assets/fonts/Inter/Inter-Medium.otf
    - family: InterBold
      fonts:
        - asset: assets/fonts/Inter/Inter-Bold.otf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
